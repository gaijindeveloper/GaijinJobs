<resources>

    <!-- for pre-Lollipop devices, any theme modifications should be added here-->
    <style name="GaijinTheme" parent="@style/GaijinThemeBase" />

    <!-- Base application theme. -->
    <style name="GaijinThemeBase" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/primary_color_dark</item>
    </style>

</resources>